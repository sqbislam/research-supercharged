import LoadingSpinner from '@/components/loading-spinner';

export default function Loading() {
  return (
    <div className='section-inner relative h-[80vh]'>
      <LoadingSpinner />
    </div>
  );
}
